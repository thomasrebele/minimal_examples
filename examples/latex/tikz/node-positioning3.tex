\documentclass{standalone}
\usepackage{tikz}
%x step={
\usetikzlibrary{positioning}
%x }
\usetikzlibrary{decorations.pathreplacing}

\begin{document}

\begin{tikzpicture}
%x description="nodes with relative positioning (3)"

%x pre={
\node[draw, gray] (a) at (0,0) {a};
%x }

%x step={
\node[draw] (b) [above right=0.5 and 1.5 of a] {b};
%x }

\tikzset{brace/.style={decorate, decoration={brace, amplitude=0.1cm}, gray}}
\draw[brace]
	(a.north east) ++(1.5,-0.15)
	-- node [below=0.2] {1.5}
	+(-1.5,0);

\draw[brace]
	(b.south west) ++ (0.15,0)
	-- node [right=0.2] {0.5}
	+(0,-0.5);

	\node[gray] at (a.north -| b.west) {+};

\end{tikzpicture}

\end{document}
