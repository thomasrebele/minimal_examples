#![allow(dead_code)]
#![allow(unused_variables)]

fn main() {

//x description="closure (2)"
//x level=8

/*
//x pre={
    let f = |x| x;

    let a = f(1);
    let b = f(true);
    // a? b?
//x }
*/

//x step={
// compile error, mismatched types
//x }

//x explanation="using a closure determines their type"
}
