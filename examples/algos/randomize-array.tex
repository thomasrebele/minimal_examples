\documentclass[varwidth]{standalone}

%x description="randomize a list"
\begin{document}

%x pre={
Input: array A of length $n$ \\
%x }

%x step={
for $i = 1..n$ \\
	$j \leftarrow rand(\{i, \ldots, n\})$ \\
	swap A[i] and A[j]
%x }


\end{document}

