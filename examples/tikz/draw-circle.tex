\documentclass{standalone}
\usepackage{tikz}

\begin{document}

% description: draw a circle
% start
\tikz {
	\draw (0,0) circle [radius=1];
	% or (old syntax)
	\draw (0,0) circle (0.5);
}
% end

\end{document}
