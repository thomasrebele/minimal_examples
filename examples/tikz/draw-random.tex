\documentclass{standalone}
\usepackage{tikz}

\begin{document}

% description: draw a random circles
\tikz {
\draw (0,0) circle (0.1);
	\foreach \x in {0,...,10} {
% start
\draw [fill] 
	% random numbers between -1 and 1
	(rand,rand) 
	circle [radius=.1];
% end
	}
}

\end{document}
