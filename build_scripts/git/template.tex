\documentclass{standalone}

\usepackage{tikz}
\usetikzlibrary{fit}
\usepackage{fontawesome}

{% raw %}
\newcommand{\branch}[1]{%
	\smash{\tikz[baseline,above=0.55mm] \node [branch] {#1};}~%
}
\newcommand{\head}{%
	\,\smash{\tikz[rounded corners=0,scale=0.25,baseline=0.2mm] \filldraw (0,0) -- (0,1) -- (0.5,0.5) -- cycle;}\,%
}
\newcommand{\file}[1]{%
	\node[file]{\faFileO\ #1};%
}
\newcommand{\content}[1]{
	\node[content] {\vphantom{l}#1};
}
\newcommand{\commitmessage}[1]{
	\node[message] {\vphantom{lg}#1};
}
{% endraw %}

\begin{document}
\begin{tikzpicture}[yscale=-1]
\tikzset{
	commit/.style={
		draw,
		rounded corners,
		fill=brown!20,
		minimum height=5mm,
	},
	message/.style={
		align=left,
		anchor=west
	},
	author/.style={
		gray
	},
	commit table/.style={
		row sep=1mm
	},
	branch/.style={
		draw,
		rounded corners,
		fill = blue!15,
		minimum height=5mm,
		inner ysep=0mm,
		anchor=mid
	},
	file/.style={
		anchor=north west,
		node font=\small,
		color=gray
	},
	content/.style={
		anchor=north west, 
		draw, 
		inner sep=1mm, 
		right=5mm,
		color=gray,
		node font=\tiny\tt
	}
}

{{tikz}}
\end{tikzpicture}

\end{document}
